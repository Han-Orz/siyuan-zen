### 思源集市没有更新
是因为name名吗？还是其他的？
可以改插件名为zentype吗？

### 聚焦模式中句级dim的动画
有没有在不改变现有 `::highlight` 基本架构的情况下实现句级 dim 透明度变化动画？

### 聚焦模式嵌套列表透明度叠加 bug（回归）
- 嵌套列表出现透明度×透明度叠加（父块 opacity × 子块 opacity → 几乎不可见）
- 退出聚焦模式（wheel/arrow/click）和关闭插件（destroyRipple）均无法恢复透明度
- 当前块其他句的轻微 dim（句级 `::highlight`）退出聚焦后也未恢复
- 疑似 `clearAll` / `destroyRipple` 只清 `modifiedBlocks` 中的块，嵌套块若不在集合中则漏清

### 打字机模式的滚动问题（待修复BUG）
- 首字不滚动,后续字滚动的BUG
- 连续键入可以不滚动,空隙时间滚动如何？
- 滚动时带着IME输入框一起,就会一卡一卡的

### 更新撰写一份详尽的项目设计文档
目的是为了让我这样的编辑小白也能理清楚插件是怎么工作的,有问题的时候能快速定位.
主要说明 + 三个模块如何工作.
