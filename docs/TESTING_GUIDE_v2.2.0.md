# 顺滑光标实际测试指南（v2.2.0）

> **环境**：`D:\SiYuan\data\plugins\siyuan-zen` 已同步完成（5/5 hash MATCH）
> **前提**：插件名 = "焦点写作"，name = `siyuan-zen`
> **测试日期**：2026-06-29

---

## 0. 重载插件（必须先做）

```
思源笔记 → 右上角 ⚙ 设置 → 插件 → 找到 "焦点写作"
→ 点击 "禁用" → 等 1 秒 → 点击 "启用"
```

或在顶栏插件图标右键 → "重载"。
或者**直接重启思源**（最彻底）。

打开 DevTools：**F12**（或 Ctrl+Shift+I），切到 Console 面板，留着备用。

---

## 1. 基础显示（4 个 Round 5 BUG 验证）

打开任一文档（最好是有多行文字的），点击正文任意位置：

| # | 期望 | 实际 | 通过？ |
|---|------|------|--------|
| 1.1 | **光标出现**（蓝色细条，约 3px 宽） | 应该立刻可见 | ☐ |
| 1.2 | **直角矩形**（无圆角） | F12 → Elements 找到 `#zentype-cursor` → 看 Computed → `border-radius: 0` | ☐ |
| 1.3 | **光标高度 ≈ 行高 × 1.1**（垂直居中） | 视觉上应略高于行高 | ☐ |
| 1.4 | **3 秒后开始呼吸闪烁** | 静止不动观察 | ☐ |
| 1.5 | **移动光标有 transition 动画**（不是瞬移） | 鼠标点击不同位置看过渡 | ☐ |

**操作中呼吸暂停**（typing 时不应呼吸）：
- 在编辑器中输入文字 → 光标停止呼吸 ✓
- 停止输入 **500ms** 后 → 应该恢复呼吸 ✓

---

## 2. 边界场景（Round 5 + Round 9 P1）

| # | 场景 | 期望 | 通过？ |
|---|------|------|--------|
| 2.1 | 点空白行（无任何文字） | 光标仍可见（fallback 起作用） | ☐ |
| 2.2 | 选中文本（拖动选中） | 光标隐藏，**选中框**显示 | ☐ |
| 2.3 | 选中文本后**取消选中**（点别的位置） | 光标重新出现 | ☐ |
| 2.4 | 进入 AV/数据库块（数据库视图） | 光标**隐藏**（AV 排除） | ☐ |
| 2.5 | 在嵌入块（iframe）里编辑 | 光标隐藏或跟随，**不闪现** | ☐ |

---

## 3. z-index 动态（Round 9 P1 + P2）

| # | 场景 | 期望 | 通过？ |
|---|------|------|--------|
| 3.1 | 打开悬浮窗（`/` 命令面板 / 块引用菜单） | 光标在悬浮窗**下方**（不遮挡菜单） | ☐ |
| 3.2 | 全屏模式（F11 或思源顶栏全屏） | 光标**仍可见**，跟随文字 | ☐ |
| 3.3 | 拖动悬浮窗 | 光标**不掉**（popover drag 处理） | ☐ |

验证方法（如果光标被遮）：
- F12 → Console 输入：`document.getElementById("zentype-cursor").style.zIndex`
- 应该返回 1000-9999 之间的数字
- 悬浮窗 z-index 通常 200-999，光标应该更高

---

## 4. ResizeObserver + 滚动（Round 9 P1）

| # | 场景 | 期望 | 通过？ |
|---|------|------|--------|
| 4.1 | **拉宽/拉窄思源窗口** | 光标位置立即更新 | ☐ |
| 4.2 | 在大文档里**滚动** | 光标跟随滚动（无瞬移） | ☐ |
| 4.3 | 滚动停止后 | 500ms 后恢复呼吸 | ☐ |

---

## 5. 分屏（Split-screen，P2 修复）

> **重点测试**：这是 P2 修的关键 BUG 之一

| # | 场景 | 期望 | 通过？ |
|---|------|------|--------|
| 5.1 | 打开 2 个文档**左右分屏** | 两边都有编辑器 | ☐ |
| 5.2 | **左边编辑 + 右边只读**（右键 → "只读"） | 左边显示光标，右边**不显示**光标 | ☐ |
| 5.3 | 点击右边只读文档 | 仍不显示光标（验证 `getActiveEditor()` 限定） | ☐ |
| 5.4 | 点击左边 | 重新显示光标 | ☐ |
| 5.5 | **切 Tab**（切到其他文档再切回） | 光标**不闪现**（`activeProtyleIds` 必须先点击才显示） | ☐ |

**5.5 是核心**：切 Tab 时旧版本会有光标短暂闪现然后隐藏，新版本应该完全不显示光标直到你点击。

---

## 6. 多文档切换（EventBus `switch-protyle`，P2）

| # | 场景 | 期望 | 通过？ |
|---|------|------|--------|
| 6.1 | 打开文档 A，在 A 中输入 | 光标跟随 | ☐ |
| 6.2 | 切到文档 B（点击 B 的 tab） | A 的光标消失（destroy 正确） | ☐ |
| 6.3 | 切回 A | A 的光标重新显示（重建正确） | ☐ |

F12 → Console → 切换时观察：
- `console.log` 应有 `EventBus switch-protyle` 触发
- 但**不应**有错误

---

## 7. 命令面板开关

按 **Ctrl+Shift+P** 打开命令面板，输入 `zenType`：

| # | 命令 | 期望 | 通过？ |
|---|------|------|--------|
| 7.1 | "zenType: 切换顺滑光标" | 光标**消失**（toggle off） | ☐ |
| 7.2 | 再次执行 | 光标**重新出现**（toggle on） | ☐ |
| 7.3 | "zenType: 切换打字机模式" | 高亮条消失/出现 | ☐ |
| 7.4 | "zenType: 切换涟漪聚焦" | 当前块高亮变化 | ☐ |

---

## 8. 配置参数修改（Round 10 config.ts）

打开 `D:\Documents\GitHub\zenType\src\config.ts`，修改某项后**重启思源**（或重新载入插件）：

```ts
CURSOR_CONFIG = {
  HEIGHT_RATIO: 1.1,    // 改成 1.5 → 光标变高
  BLINK_DELAY_MS: 500,  // 改成 1500 → 呼吸间隔变长
  ...
}
```

| # | 修改 | 期望 | 通过？ |
|---|------|------|--------|
| 8.1 | `HEIGHT_RATIO = 1.5` | 光标高度明显增加 | ☐ |
| 8.2 | `BLINK_DELAY_MS = 1500` | 输入后 1.5s 才开始呼吸 | ☐ |
| 8.3 | 改完 `node build.js --dev` | 自动重建 dev/ | ☐ |
| 8.4 | 重启示源 | 新参数生效 | ☐ |

如果第 8.1/8.2 不生效：
- 检查 `pnpm run dev` 是否在跑（应该监听文件改动自动重建）
- 检查思源插件是否真的重载（看版本号是否变）
- F12 → Console 看有没有 JS 错误

---

## 9. 已知限制（不需要修复）

| # | 限制 | 表现 | 影响 |
|---|------|------|------|
| 9.1 | `SMOOTH_ENABLED` / `BLINK_ENABLED` 等 4 个配置**编译期锁死** | 修改 config.ts 这些字段无效 | 不影响功能，等后续迭代 |
| 9.2 | `__zentypeScrollBound` 在 toggle off→on 后残留 | 重绑滚动容器时跳过已绑元素 | 实际无影响（bindScrollContainerEvents 每次 doUpdateCursor 都重遍历） |
| 9.3 | 移动端 `isMobile()` 用 `getFrontend()` | 移动端可能不显示光标标题（按需） | 移动端体验尚未优化 |

---

## 10. 调试技巧（如果出问题）

### F12 Console 看错误
- 红色 `Error` 必报
- 黄色 `Warning` 看是否与 `zentype` 相关

### F12 Elements 检查 DOM
```
Ctrl+F 搜 "zentype-cursor"
```
- 应找到 `<div id="zentype-cursor" class="hidden ...">`
- 如果 `class` 包含 `hidden` 但光标可见 → CSS 问题
- 如果元素根本不存在 → 模块未初始化

### F12 Application → Storage → Local Storage
- 看是否有 `siyuan-zen` 相关条目
- 正常应该为空（v2 不存任何数据）

### 强制重新初始化
```
思源 → 设置 → 插件 → 焦点写作 → 禁用 → 启用
```
如果还不行：直接**重启思源**。

### 看 EventBus 是否订阅成功
F12 Console 输入：
```js
window.siyuan.eventBus
```
应该返回一个对象（具体结构看思源版本）。

---

## 11. 测试结果汇报模板

测试完后请告诉我：

```
# 1. 基础显示（5 项）
1.1 光标出现: ✅/❌ (现象)
1.2 直角矩形: ✅/❌
...

# 2. 边界场景（5 项）
...

# 3. z-index 动态（3 项）
...

# 5. 分屏（重点测试，5 项）
5.1 两边都打开: ✅/❌
5.5 切 Tab 不闪现: ✅/❌ (重点)
...

# 8. 配置修改（4 项）
...

# 发现的问题
- [描述现象 + 复现步骤]
```

如果某个测试项失败，**截图（F12 + 截屏键）+ 描述场景**比文字描述更有效。

---

## 12. 测试完后的下一步

| 你的反馈 | 我的下一步 |
|---------|-----------|
| 全部通过 | 准备 git commit（首次），commit 信息：v2.2.0 |
| 1-2 个小问题 | 单独 dispatch deep-worker 修复，commit v2.2.1 |
| 大问题（如光标不显示） | 重新进入 oracle → deep-worker → reviewer 链 |
| 切 Tab 闪现 | 重新审查 `activeProtyleIds` 实现 |
| 配置修改不生效 | 检查 `pnpm run dev` 是否在跑 + 思源是否真重载 |

---

**预计测试时间**：10-15 分钟（含重启示源等待）